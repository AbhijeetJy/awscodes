git config --global user.name "username"
git config --global user.email "email"

git log
git checkout copyid8digits filename.txt
git remote add origin "url of de["
git pull origin master
git push origin master
git pull --rebase origin master
git mergetool
c. development-git,SUN
C. integration-jenkins,teamcity
c. testing-testmg,junit
c deployment - chef,puppet
c monitoring - splunk,negios
